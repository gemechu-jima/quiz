{
  "name": "quize",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client && npm start",
    "start-app": "cd client && npm run build",
    "server": "cd backend && npm start",
    "dev": "npm-run-all --parallel start server",
    "setup-app": "npm-run-all --parallel start-app server"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "npm-run-all": "^4.1.5"
  }
}
