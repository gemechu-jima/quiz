{
  "name": "quize",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client && npm install && npm start",
    "start-app": "npm i && cd client && npm i && npm run build",
    "server": "cd backend && npm i && npm start",
    "dev": " concurrently \"npm start\" \"npm run server\""
    
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "npm-run-all": "^4.1.5"
  }
}
