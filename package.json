{
  "name": "quize",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": " cd Client && npm install && npm start",
    "start-app": "npm install && cd Client && npm install && npm run build",
    "server": "cd backend && npm install && npm start",
    "dev":" concurrently \"npm start\" \"npm run server\"",
    "setup-app": "concurrently  \"npm run start-app\" \"npm run server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
