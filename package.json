{
  "name": "quize",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client && npm install && npm start",
    "start-app": "npm install && cd client && npm install && npm run build",
    "server": "cd backend && npm install && npm start",
    "dev": "npm-run-all --parallel start server",
    "setup-app": "npm-run-all --parallel start-app server"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "npm-run-all": "^4.1.5"
  }
}
